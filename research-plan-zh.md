# ConstructionSceneReady 研究计划

## 1. 最终研究判断

这项工作不应再以“IFC 转 USD”“BIM 导入 Isaac Sim”或“用 Agent
生成机器人仿真场景”为核心创新。这些单点能力已经或正在成为基础设施。

本文应研究一个更明确、可检验且与既有研究连续的问题：

> **如何把建筑尺度的 IFC 设计信息编译成任务激活、物理可执行、可被确定性规则验证的多尺度机器人场景，并使场景级合格性能够预测人形机器人装配任务能否成功？**

NVIDIA 当前的 SimReady 体系已经能够验证单个资产和 Feature，但官方文档明确说明：
完整组合场景尚缺少成熟的场景级规范和静态验证器。因此，本文的主要空白是
**construction task scene readiness**，而不是再造一个通用资产检查器。

建议论文名称：

> **ConstructionSceneReady: Multi-scale IFC-to-OpenUSD compilation and
> scene-level validation for humanoid robotic assembly**

中文名称：

> **ConstructionSceneReady：面向人形机器人装配的多尺度
> IFC—OpenUSD 场景编译与任务级验证方法**

## 2. 科学问题与研究假设

### RQ1：通用资产级 SimReady 规则遗漏了哪些建造任务级场景缺陷？

关注的不是单个构件是否有质量、碰撞体或语义标签，而是多个资产组合后是否满足：

- IFC 构件与 USD Prim 身份一致；
- 机器人、工具、构件和接口引用闭合；
- 装配前置条件与施工状态一致；
- 门洞、通行区、工作区和接触接口采用合适精度；
- 任务目标可达、可抓取、可对齐和可插入；
- 临时状态在物理上稳定；
- 参数来源、置信度和不确定性可以追溯。

**H1：** 通用资产级验证对建造场景关键错误的召回率显著低于本文场景级验证。

### RQ2：任务激活式多尺度编译能否同时保证效率和接口精度？

场景分为：

1. `Global building layer`：建筑壳体、门洞、通行区、简化碰撞；
2. `Active work-zone layer`：材料区、障碍、机器人站位和中精度几何；
3. `Interaction layer`：孔位、销轴、装配轴、公差、接触和临时约束。

**H2：** 按任务加载 OpenUSD Payload，可以降低载入时间、内存和碰撞检测成本，
且不降低当前任务所需的接口精度和成功率。

### RQ3：验证器约束的 Agent 修复是否优于无约束生成？

Agent 只能读取结构化错误报告并调用白名单工具，不能直接任意重写整个场景。

**H3：** 验证器约束的修复在最终合格率、错误修复率和重复运行一致性方面优于
纯规则转换和无验证 LLM/Agent。

### RQ4：场景级 readiness 是否能预测机器人任务成功？

**H4：** 关键规则通过率和场景 readiness 分数与机器人完整任务成功率显著相关，
且能够定位失败来自场景模型而非控制器。

## 3. 论文的三项核心贡献

1. **Construction Scene Readiness Contract**  
   定义机器人、构件、装配接口、施工状态、任务、工具、空间分层和参数来源之间的
   场景级约束；它建立在通用 SimReady 资产规则之上，不重复资产级标准。

2. **任务激活式多尺度 IFC—OpenUSD 编译器**  
   将 IFC4.3、工序和装配接口编译为全局层、工作区层和交互层，并保留 IFC GUID、
   物理属性、任务语义、精度等级和来源信息。

3. **确定性验证与受约束 Agent 修复 Benchmark**  
   通过已知真值的缺陷注入，比较直接转换、规则转换、无约束 Agent 和本文方法，
   并最终在 Unitree G1 人形机器人装配仿真中检验场景质量是否影响任务成功。

## 4. 机器人与典型任务

### 主平台

- Unitree G1 29-DOF 人形机器人；
- 第一阶段使用双指夹爪，避免灵巧手控制干扰场景研究；
- Dex3 仅作为后续扩展或补充实验；
- 第一篇不训练通用 VLA，也不把强化学习作为主要贡献。

### 主任务链

> 取件 → 穿越门洞/低门槛 → 到达工作区 → 双手定位 →
> 孔轴对齐 → 简化销轴插入 → 更新施工状态

### 三个实验等级

1. **Level 1：固定站姿局部装配**  
   验证语义、接口、碰撞、质量、惯量、插入轴和状态。
2. **Level 2：原地全身操作**  
   加入下蹲、起身、转腰、双手扶持和不同高度作业。
3. **Level 3：建筑尺度移动操作**  
   加入材料区、门洞、门槛、临时障碍、站位规划和完整任务链。

只有 Level 1 跑通后才进入 Level 2；只有论文核心结果已经成立，才把 Level 3
作为强实验加入。这样人形仿真难度不会拖垮方法论文。

## 5. 方法工作包

### WP0：证据化文献与新颖性审计

建立 40–60 篇核心文献矩阵，字段至少包括：

- IFC/BIM 输入；
- USD/SDF/MJCF 输出；
- 资产级物理属性；
- 建造语义与施工状态；
- 多尺度场景组织；
- 场景级静态验证；
- 运行时任务验证；
- 不确定性与来源；
- Agent 诊断/修复；
- 人形机器人实验。

投稿前必须重新审计一次，不能使用“首次”作为未经证实的主张。

### WP1：Construction Scene Intermediate Representation

先采用 JSON Schema/Python 数据类实现，不急于上图数据库。核心实体：

- Component；
- Assembly interface；
- Construction state；
- Robot；
- Tool；
- Task；
- Work zone；
- Provenance record。

### WP2：参数化 IFC4.3 典型场景

首批自己生成三个基础场景：

1. 连接板定位；
2. 销轴插入；
3. 两构件顺序装配。

每个模型都由代码同时生成 ground truth，避免手工标注。

### WP3：多尺度 IFC—OpenUSD 编译

- IFC/IfcOpenShell 解析；
- IFC GUID 与 USD Prim 映射；
- 建筑层、工作区层、交互层；
- Reference/Payload 组织；
- 质量、质心、惯量和碰撞属性；
- 任务、接口、状态和来源元数据；
- 可重复的编译报告。

### WP4：场景级规则与验证器

第一版控制在 20–30 条规则，分为：

1. 身份与引用闭合；
2. 空间、单位与坐标；
3. 装配接口与容差；
4. 施工状态与状态转换；
5. 机器人能力与任务前置条件；
6. 物理属性与临时稳定；
7. 来源、不确定性与人工确认门槛；
8. 多尺度载荷与精度一致性。

### WP5：缺陷注入与 Agent 修复

注入六类已知错误：

- 语义错误；
- 几何/坐标错误；
- 物理参数错误；
- 接口与容差错误；
- 状态转换错误；
- 任务/机器人引用与可执行性错误。

Agent 只能调用：

- 查看 IFC/场景实体；
- 重算质量和惯量；
- 重建碰撞体；
- 修复映射；
- 修复接口；
- 调整允许范围内的参数；
- 重新运行验证；
- 将高风险问题升级为人工确认。

### WP6：RTX 运行时验证

在 Ubuntu RTX 节点上执行：

- Isaac Sim 场景加载与资源统计；
- PhysX 稳定、碰撞、接触和约束；
- G1 可达性与全身任务；
- 抓取、对齐、插入和完整任务成功率；
- 少量 RTX 相机结果；
- 修复前后运行时差异。

## 6. 本机马上可以开始的研究

本机为 Intel i9 8 核、32 GB RAM、AMD Radeon Pro 5500M 8 GB。可立即开展：

1. 文献矩阵和不可声称创新清单；
2. 研究问题、假设、指标和实验协议；
3. 参数化 IFC 场景生成与 IfcOpenShell 解析；
4. 中间表示和 JSON Schema；
5. 场景级静态规则；
6. 缺陷注入器与 ground truth；
7. OpenUSD/USDA 层级和 Payload 生成；
8. 质量、惯量、接口轴和容差的确定性计算；
9. Agent 的白名单工具与修复状态机；
10. 单元测试、CPU Benchmark、统计脚本；
11. AIC 论文第 1–6 节、Highlights、Graphical Abstract 草图。

本机不承担：

- Isaac Sim 最终实验；
- PhysX GPU 接触和复杂约束；
- RTX 相机/激光雷达；
- G1 全身移动操作；
- Isaac Lab 并行训练；
- 最终仿真视频。

预计 65%–75% 的方法、代码、数据和写作可在本机完成。RTX 节点应采用
NVIDIA 官方支持版本的原生 Ubuntu，并把驱动、CUDA、Isaac Sim 和容器版本锁死。

## 7. 前四周执行计划

### 第 1 周：论文先行

- 冻结题目、RQ、假设和三项贡献；
- 建立文献矩阵；
- 完成 AIC Introduction 的问题链和 Related Work 表格结构；
- 确定三种基础任务和最小数据字段。

### 第 2 周：场景契约

- 完成中间表示 v0.1；
- 编写 10 条场景规则；
- 制作一个正确和一个错误样例；
- 形成结构化验证报告。

### 第 3 周：IFC 最小闭环

- 安装 IfcOpenShell；
- 代码生成三个小型 IFC4.3 模型；
- IFC → 中间表示；
- 自动记录 GUID、材料、接口、状态和 ground truth。

### 第 4 周：缺陷与修复

- 实现至少三类缺陷注入；
- 实现白名单修复工具；
- 完成一次“失败 → 诊断 → 修复 → 再验证”；
- 输出第一张方法图、第一张数据表和本地基准结果。

## 8. 四周停止线

同时满足以下条件才扩大项目：

1. 三个 IFC fixture 能稳定进入中间表示；
2. 至少十条规则可重复运行；
3. 至少五条建造场景规则属于通用资产检查覆盖不到的场景关系；
4. 至少一个缺陷能被受约束修复流程正确修复；
5. 结果可由一个命令复现并生成 JSON/CSV 报告。

若第 4 条失败，先发表/推进“场景规范与验证”而不强行加入 Agent。
若第 5 条失败，停止扩展 G1 和大型 IFC 场景，先解决复现性。

## 9. 实验设计

### 基线

- B1：IFC 几何直接转换；
- B2：固定规则式 IFC→USD；
- B3：无确定性验证的 LLM/Agent 修复；
- Ours：场景契约 + 多尺度编译 + 确定性验证 + 受约束修复。

### 消融

- 去掉多尺度层；
- 去掉 provenance；
- 去掉 task-level rules；
- 去掉 Agent；
- 去掉 construction-state rules。

### 指标

- 语义保留率和关系 F1；
- 关键错误 Precision/Recall/F1；
- 首次/最终修复成功率；
- 错误修复率与人工介入率；
- 编译、验证、修复时间；
- USD 载入时间、峰值内存和活动 Prim 数；
- 可达、无碰撞、抓取、对齐、插入和完整任务成功率；
- readiness 分数对任务成功的预测能力。

### 统计

- 配对场景设计；
- Bootstrap 95% 置信区间；
- McNemar 检验比较成功/失败；
- Wilcoxon 检验比较时间和迭代次数；
- Logistic regression 分析 readiness 与任务成功率。

## 10. 论文图表计划

### 主图

1. 研究空白：资产级 SimReady 与场景级 Construction readiness；
2. 总体框架；
3. 场景中间表示；
4. 全局—工作区—交互多尺度 USD 结构；
5. 场景级规则体系；
6. 验证器约束的修复循环；
7. 三类任务与六类缺陷；
8. 错误检测和修复结果；
9. G1 典型任务全过程；
10. readiness 分数与任务成功关系。

### 主表

1. 文献比较矩阵；
2. 场景契约与规则；
3. Benchmark 构成；
4. 基线与消融；
5. 指标定义；
6. 编译与语义保留；
7. 错误检测；
8. 修复结果；
9. 机器人任务结果；
10. 计算成本和敏感性。

## 11. AIC 写作策略

- 从第一天使用 Elsevier `elsarticle`，正文英文；
- 主文档匿名，作者、单位、致谢和基金放独立 title page；
- Highlights 单独保存，3–5 条，每条不超过 85 个字符；
- Introduction 用“问题—缺口—研究问题—贡献”结构；
- Methods 与 Implementation 分开；
- 结果未完成前只写实验协议，不预填任何结果；
- 代码、数据、环境版本和 AI 使用声明从项目开始即记录；
- 投稿前重新核对 AIC Guide for Authors，因为具体要求可能更新。

## 12. 风险控制

- **风险：题目过大。**  
  先完成 Level 1，Level 2/3 是增强实验。
- **风险：Agent 贡献虚。**  
  Agent 只能修复由确定性规则定位的错误，并与无验证 Agent 对照。
- **风险：SimReady 名称造成官方标准误解。**  
  项目名使用 ConstructionSceneReady，明确它是研究原型，不声称为官方认证 Profile。
- **风险：控制器失败污染场景评价。**  
  固定机器人、控制器、随机种子和任务轨迹；用配对场景只改变场景缺陷。
- **风险：本机无法跑 Isaac。**  
  所有核心逻辑先做成纯 Python/CPU；RTX 只作为后端运行时。

